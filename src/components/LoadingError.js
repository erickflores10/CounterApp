import React from "react";

const LoadingError = () => {
    return(
        <span style={{
            padding: "15px",
            background: "#f8d7da"
        }}>Error en la carga</span>
    );
}

export default LoadingError;